name: azure-provider
version: 0.0.@
description: 
logo: 

provides:
- azure

includes:
- README.md

inputs:
- id: credentials
  sensitive: true
  description: Credentials for the service principal (json)

outputs:
- id: app_id
  default: "00000000-0000-0000-0000-000000000000"

- id: display_name
  default: 

build: 
deploy:

