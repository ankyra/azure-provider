name: azure-cli
version: 0.1.@
logo: 
description: |
  Makes sure the 'az' binary is available during deployment. This release uses
  `apt-get` to install the azure-cli if it can't be found on the PATH.

provides:
- azure-cli

includes:
- README.md

build: install_az.py
deploy: install_az.py 
activate: install_az.py
