name: azure-cli
version: 0.0.@
logo: 
description: |
  Makes sure the 'az' binary is available during deployment.

provides:
- azure-cli

includes:
- README.md

build: install_az.py
deploy: install_az.py 
activate: install_az.py
